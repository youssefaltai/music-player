import styled from "styled-components";

const PlaylistTrackArtistStyle = styled.div`
  color: var(--light3);
  font-size: 14px;
  font-weight: normal;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`;

export default PlaylistTrackArtistStyle;

