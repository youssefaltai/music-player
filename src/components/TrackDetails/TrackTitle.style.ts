import styled from "styled-components";

const TrackTitleStyle = styled.div`
  font-size: 24px;
  font-weight: bold;
  margin-bottom: 8px;
  color: var(--text-color);
`;

export default TrackTitleStyle;
